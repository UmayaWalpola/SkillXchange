APPLICATION WORKFLOW - VISUAL REFERENCE GUIDE
==============================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                         USER SIDE - APPLICATION FLOW                        │
└─────────────────────────────────────────────────────────────────────────────┘

1. USER OPENS PROJECT PAGE
   └─→ URL: /project/detail/12
       └─→ File: app/views/projects/view.php
           └─→ Shows project details + application form

2. USER SEES APPLICATION SECTION
   ┌──────────────────────────────────────────┐
   │ ✨ Join This Project                     │
   │                                          │
   │ [Apply to Join This Project]             │
   │                                          │
   │ [Form Hidden]                            │
   └──────────────────────────────────────────┘

3. USER CLICKS BUTTON
   └─→ JavaScript toggles form visibility
       └─→ Form appears with textarea

4. USER FILLS FORM
   ┌──────────────────────────────────────────┐
   │ Tell us why you'd be a great fit:        │
   │                                          │
   │ ┌────────────────────────────────────┐   │
   │ │ I have 5 years of experience...    │   │
   │ │ and I'm very interested...         │   │
   │ └────────────────────────────────────┘   │
   │                                          │
   │ [Submit Application] [Cancel]            │
   └──────────────────────────────────────────┘

5. USER SUBMITS
   └─→ Form POSTs to: /ProjectApplication/apply/12
       └─→ Method: POST
           └─→ Data: message="I have 5 years..."

6. CONTROLLER PROCESSES
   ┌─ ProjectApplicationController::apply(12)
   ├─ Validate user is logged in ✓
   ├─ Validate user is not organization ✓
   ├─ Validate project exists ✓
   └─ Call Project::applyToProject(12, userId, message)
      ├─ Check for duplicate (none found) ✓
      └─ INSERT INTO project_applications
         └─ INSERT VALUES (project_id=12, user_id=5, message=..., status='pending')

7. APPLICATION STORED IN DB
   ┌──────────────────────────────────────────┐
   │ project_applications Table               │
   ├──────────────────────────────────────────┤
   │ id=100                                   │
   │ project_id=12                            │
   │ user_id=5                                │
   │ message="I have 5 years..."              │
   │ status="pending"                         │
   │ applied_at=2025-11-18 10:30:00           │
   └──────────────────────────────────────────┘

8. SUCCESS RESPONSE
   └─→ Redirect to: /project/detail/12
       └─→ Show message: "Application submitted successfully!"
           └─→ Page shows status: "Application Status: pending"

┌─────────────────────────────────────────────────────────────────────────────┐
│                    ORGANIZATION SIDE - REVIEW FLOW                          │
└─────────────────────────────────────────────────────────────────────────────┘

1. ORGANIZATION OPENS APPLICATIONS
   └─→ URL: /organization/applications
       └─→ File: app/views/organization/applications.php
           └─→ Controller: OrganizationController::applications()

2. CONTROLLER FETCHES DATA
   ┌─ OrganizationController::applications()
   ├─ Get org_id from session
   ├─ Call Project::getAllApplicationsForOrganization(org_id)
   │  └─→ Query:
   │     SELECT pa.*, p.name, u.username, u.email, 
   │            GROUP_CONCAT(us.skill_name) AS user_skills,
   │            COUNT(completed_projects) ...
   │     FROM project_applications pa
   │     JOIN projects p ON pa.project_id = p.id
   │     JOIN users u ON pa.user_id = u.id
   │     LEFT JOIN user_skills us ON ...
   │     WHERE p.organization_id = org_id
   │
   └─ Call Project::getApplicationStats(org_id)
      └─→ Query:
         SELECT 
           COUNT(*) AS total,
           SUM(CASE WHEN status='pending' THEN 1) AS pending,
           SUM(CASE WHEN status='accepted' THEN 1) AS accepted,
           SUM(CASE WHEN status='rejected' THEN 1) AS rejected

3. DATA PASSED TO VIEW
   ┌──────────────────────────────────────────────────────────┐
   │ $data = [                                                │
   │   'applications' => [                                    │
   │     {id: 100, user_name: 'john', status: 'pending',..}, │
   │     {id: 99, user_name: 'jane', status: 'accepted',...},│
   │     ...                                                  │
   │   ],                                                     │
   │   'stats' => {total: 5, pending: 2, accepted: 2,...}    │
   │ ]                                                        │
   └──────────────────────────────────────────────────────────┘

4. ORGANIZATION SEES STATS
   ┌─────────────────────────────────────────────────────────┐
   │                   Project Applications                   │
   ├─────────────────────────────────────────────────────────┤
   │                                                          │
   │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐               │
   │  │   5  │  │   2  │  │   2  │  │   1  │               │
   │  │ TOTAL│  │PENDING│  │ACCEPTED│ REJECTED│             │
   │  └──────┘  └──────┘  └──────┘  └──────┘               │
   │                                                          │
   └─────────────────────────────────────────────────────────┘

5. ORGANIZATION SEES PENDING APPLICATIONS
   ┌─────────────────────────────────────────────────────────┐
   │ ⏳ Pending Applications                                  │
   ├─────────────────────────────────────────────────────────┤
   │                                                          │
   │ [Avatar] john_doe (john@example.com)                   │
   │          ⭐ Rating: 4.8 • 👥 Completed: 12             │
   │                                                          │
   │ Skills: Python | TensorFlow | Scikit-Learn             │
   │                                                          │
   │ Message:                                                │
   │ "I have 5 years of ML experience and I'm very          │
   │  interested in contributing to this project."           │
   │                                                          │
   │ Project: Machine Learning Model                         │
   │ [Accept] [Reject]                                       │
   │                                                          │
   │ ─────────────────────────────────────────────────────── │
   │                                                          │
   │ [Avatar] jane_smith (jane@example.com)                 │
   │          ⭐ Rating: 4.5 • 👥 Completed: 8              │
   │                                                          │
   │ Skills: Python | Data Analysis | SQL                    │
   │                                                          │
   │ Message:                                                │
   │ "I'm looking to expand my ML skills and think..."      │
   │                                                          │
   │ Project: Data Analysis Platform                         │
   │ [Accept] [Reject]                                       │
   │                                                          │
   └─────────────────────────────────────────────────────────┘

6. ORGANIZATION CLICKS ACCEPT
   └─→ Link: /organization/handleApplication/100/accept
       └─→ OrganizationController::handleApplication(100, 'accept')
           ├─ Call Project::acceptApplication(100, org_id)
           │  ├─ Load application and verify
           │  ├─ Load project and verify ownership
           │  ├─ Check capacity
           │  ├─ Transaction begin
           │  ├─ INSERT INTO project_members (project_id, user_id, status='active')
           │  ├─ UPDATE project_applications SET status='accepted'
           │  ├─ UPDATE projects SET current_members = current_members + 1
           │  ├─ Transaction commit
           │  └─ Return success
           └─ Redirect to /organization/applications
               └─ Show: "Application accepted!"
                   └─ Application moves to "Accepted Applications" section

7. VIEW UPDATES DYNAMICALLY
   ┌─────────────────────────────────────────────────────────┐
   │ ⏳ Pending Applications                                  │
   ├─────────────────────────────────────────────────────────┤
   │ [Avatar] jane_smith (jane@example.com)  ← john_doe gone│
   │          ⭐ Rating: 4.5 • 👥 Completed: 8              │
   │ ...                                                      │
   └─────────────────────────────────────────────────────────┘
   
   ┌─────────────────────────────────────────────────────────┐
   │ ✅ Accepted Applications                                │
   ├─────────────────────────────────────────────────────────┤
   │ [Avatar] john_doe (john@example.com)  ← john_doe moved│
   │          ⭐ Rating: 4.8 • 👥 Completed: 12             │
   │ ...                                                      │
   │ Accepted                                                 │
   └─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          DATABASE STRUCTURE                                 │
└─────────────────────────────────────────────────────────────────────────────┘

users (individual)
├── id
├── username
├── email
├── profile_picture
└── bio

          ↓ applies to

projects
├── id
├── organization_id  ←─┐
├── name              │
├── description       │
└── max_members       │
                      │
            organization
            ├── id ─────┘
            ├── name
            └── email

          ↓ references

project_applications  ← THIS IS WHERE APPLICATIONS LIVE
├── id (PRIMARY KEY)
├── project_id (FK) ────┐
├── user_id (FK) ───────┐
├── message            │
├── status             │
└── applied_at         │
                       │
     ├─→ projects table
     └─→ users table

project_members (created when application accepted)
├── id
├── project_id
├── user_id
├── role
├── joined_at
└── status

┌─────────────────────────────────────────────────────────────────────────────┐
│                              STATUS FLOW                                    │
└─────────────────────────────────────────────────────────────────────────────┘

Application Created
       ↓
   PENDING ← User is applying
       ↓
   ┌───┴────┐
   ↓        ↓
ACCEPTED  REJECTED
   ↓
Member added to project_members table
